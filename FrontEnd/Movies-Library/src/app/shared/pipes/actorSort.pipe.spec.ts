import { ActorSortPipe } from './actorSort.pipe';

describe('Pipe: ActorSorte', () => {
  it('create an instance', () => {
    let pipe = new ActorSortPipe();
    expect(pipe).toBeTruthy();
  });
});
